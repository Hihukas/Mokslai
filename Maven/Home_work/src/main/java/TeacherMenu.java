import java.util.Map;
import java.util.Scanner;

public class TeacherMenu implements Menu {
    @Override
    public void menu(Scanner scanner, Map<String, String> users) {
        /*String action;
        do {
            System.out.println("""
                    Pasirinkite norimą atlikti veiksmą:
                    [1] - Testų rezultatai.
                    [2] - Sukurti testą.
                    [3] - Testų redagavimas.
                    [4] - Išeiti.
                    """);
            action = scanner.nextLine();
            switch (action) {
                case "1" -> (scanner,users);
                case "2" -> (scanner,users);
                case "3" -> (scanner,users);
                case "4" -> System.out.println("Programa baigia darbą.");
                default -> System.out.println("Tokio veiksmo nėra. Pasirinkite iš naujo.");
            }
        } while (!action.equals("1") && !action.equals("2") && !action.equals("3") && !action.equals("4"));*/
    }
}
